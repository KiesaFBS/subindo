<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabri.madtorneio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_torneiohdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_torneiohdpi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.a1PrincipalActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.a2QtdtimesActivity" />
        <activity android:name=".Activity.a3NomeTimesTorneioActivity" />
        <activity android:name=".Activity.a4dueloTorneiosActivity" />
        <activity android:name=".Activity.testeActivity" />
        <activity android:name=".Activity.a41ganhou"></activity>
    </application>

</manifest>